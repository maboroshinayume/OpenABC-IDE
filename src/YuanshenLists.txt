SET(TARGET_NAME "YuanshenQidong")

SET(CRITICAL_PATH    ${CMAKE_SOURCE_DIR}/Genshin/runner.exe)
LIST(APPEND DIR_PATH_TEST
    ./
    ${CRITICAL_PATH}
    )

INCLUDE_DIRECTORIES(${DIR_PATH_TEST})	
LIST(APPEND SRC_FILES_TEST 
    cecide.cpp
    main.cpp
)

ADD_EXECUTABLE(${TARGET_NAME}
    ${SRC_FILES_TEST}
    )

TARGET_LINK_LIBRARIES(${TARGET_NAME}
    Mihoyo
    )

INSTALL(TARGETS ${TARGET_NAME} RUNTIME DESTINATION /Mihoyo/Runner)

